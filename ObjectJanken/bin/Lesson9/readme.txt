オブジェクト指向分析設計　6月13日課題	16M11777	菰田 徹也

基本課題）
	1.	receiveCard()メソッドはPlayerスーパークラスのreceiveCard()メソッドをそのまま使用した．
	2.	play()メソッドはfantanPlayerクラスのplay()メソッドを利用した．おけるカードがない場合にはPageOneMasterクラスに用意されているpass()メソッドを実行する．その際，Playerスーパークラスで用意されているmaster_フィールドはMasterスーパークラスのものであり，PageOneMasterサブクラスで追加したpass()メソッドを実行するためにmaster_フィールドをPageOneMasterサブクラスにキャストしてある．

応用課題)
	